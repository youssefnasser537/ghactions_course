name: Echo Job

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  echo-job:
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: echo "✅ Workflow echo.yml triggered!"

